<section class="about" id="about" aria-labelledby="about-heading" data-bg="light">
  <div class="about__inner">
    <div class="about__body">
      <div class="about__image-col" data-reveal aria-hidden="true">
        <img
          src={`${import.meta.env.BASE_URL}/images/stefan-about-us.webp`}
          srcset={`${import.meta.env.BASE_URL}/images/stefan-about-us.webp 900w`}
          sizes="(max-width: 860px) 100vw, 50vw"
          alt=""
          width="900"
          height="1200"
          loading="lazy"
          decoding="async"
        />
      </div>
      <div class="about__content">
        <h2 class="about__headline" id="about-heading" data-reveal>
          WE STARTED<br />WITH <span class="about__accent">ONE</span><br />DRUM.
        </h2>
        <div class="about__right" data-reveal data-delay="1">
          <div class="about__text">
            <p>
              We started with a single drum roaster, a notebook of cupping scores, and a stubborn
              belief that great coffee should taste like the place it came from, not like itself.
              A decade on, we still roast every batch by ear.
            </p>
            <p>
              Pulled on a 1969 Faema E61 we restored over two winters. Sourced from farmers we
              know by name and visit each year. Roasted that morning, pulled to order in front of you.
            </p>
          </div>

          <dl class="about__facts">
            <div class="about__fact">
              <dt><span class="about__fact-num" aria-hidden="true">01</span>A Decade On</dt>
              <dd>Trading since 2015. Same drum roaster, same farmers, sharper ear.</dd>
            </div>
            <div class="about__fact">
              <dt><span class="about__fact-num" aria-hidden="true">02</span>Single Origin</dt>
              <dd>One farm at a time, sourced direct. Rotated seasonally; ask what's on today.</dd>
            </div>
            <div class="about__fact">
              <dt><span class="about__fact-num" aria-hidden="true">03</span>By Ear</dt>
              <dd>No automated roast profiles. Colour, aroma, and sound guide every batch.</dd>
            </div>
            <div class="about__fact">
              <dt><span class="about__fact-num" aria-hidden="true">04</span>The Machine</dt>
              <dd>A 1969 Faema E61, restored by hand over two winters. Pulls every shot.</dd>
            </div>
          </dl>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about {
    background-color: var(--paper);
    color: var(--ink);
    padding: 3.5rem 0 7rem;
    overflow-x: clip;
    border-bottom: 2px solid var(--ink);
  }

  .about__inner {
    max-width: 72rem;
    margin-inline: auto;
    padding-inline: 2.5rem;
  }

  .about__body {
    display: grid;
    grid-template-columns: 1fr 1.6fr;
    gap: 4rem;
    align-items: stretch;
  }

  .about__image-col {
    overflow: hidden;
    border-radius: 2px;
  }

  .about__image-col img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 10%;
    filter: grayscale(100%) contrast(1.1) brightness(0.85);
    display: block;
  }

  .about__content {
    display: flex;
    flex-direction: column;
    gap: 3rem;
    padding-top: 1rem;
  }

  .about__headline {
    font-family: var(--font-display);
    font-size: clamp(3.75rem, 8.5vw, 7rem);
    font-weight: 900;
    line-height: 1.05;
    letter-spacing: -0.02em;
    color: var(--ink);
  }

  .about__accent {
    color: var(--brick-word);
  }

  .about__right {
    display: flex;
    flex-direction: column;
    gap: 3rem;
  }

  .about__text {
    display: flex;
    flex-direction: column;
    gap: 1.375rem;
  }

  .about__text p {
    font-size: 1rem;
    font-weight: 400;
    line-height: 1.7;
    color: var(--ink-body);
    max-width: 52ch;
  }

  .about__facts {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem 2.5rem;
  }

  .about__fact {
    padding: 0;
  }

  .about__fact dt {
    font-family: var(--font-display);
    font-size: clamp(1.125rem, 2.5vw, 1.625rem);
    font-weight: 900;
    letter-spacing: 0.01em;
    text-transform: uppercase;
    color: var(--ink);
    line-height: 0.95;
    margin-bottom: 0.625rem;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .about__fact-num {
    font-family: var(--font-body);
    font-size: 0.625rem;
    font-weight: 500;
    letter-spacing: 0.14em;
    color: var(--ink-subtle);
    text-transform: none;
  }

  .about__fact dd {
    font-family: var(--font-body);
    font-size: 0.8125rem;
    font-weight: 400;
    line-height: 1.55;
    color: var(--ink-subtle);
  }

  @media (max-width: 860px) {
    .about__body {
      grid-template-columns: 1fr;
      gap: 2.5rem;
    }

    .about__image-col {
      position: static;
    }

    .about__image-col img {
      aspect-ratio: 4 / 3;
      object-position: center 20%;
    }

    .about__text p {
      max-width: 60ch;
    }
  }

  @media (max-width: 640px) {
    .about {
      padding: 2.5rem 0 5rem;
    }

    .about__inner {
      padding-inline: 1.25rem;
    }

    .about__facts {
      grid-template-columns: 1fr;
    }

  }

  @supports (animation-timeline: scroll()) {
    .about__image-col img {
      animation: img-parallax linear both;
      animation-timeline: view();
      animation-range: entry 0% exit 100%;
    }

    @keyframes img-parallax {
      from { transform: scale(1.06) translateY(-3%); }
      to   { transform: scale(1.06) translateY(3%); }
    }

    .about__headline {
      animation: about-headline-in linear both;
      animation-timeline: view();
      animation-range: entry 0% entry 55%;
    }

    @keyframes about-headline-in {
      from { opacity: 0; transform: translateX(-4rem); }
      to   { opacity: 1; transform: none; }
    }

    .about__right {
      animation: about-right-in linear both;
      animation-timeline: view();
      animation-range: entry 15% entry 65%;
    }

    @keyframes about-right-in {
      from { opacity: 0; transform: translateY(2rem); }
      to   { opacity: 1; transform: none; }
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .about__image-col img {
      animation: none;
    }
  }
</style>
