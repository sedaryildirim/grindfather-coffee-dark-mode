<footer class="footer" role="contentinfo">
  <div class="footer__bar">
    <a href="#" class="footer__wordmark" aria-label="GRINDFATHER Coffee Co. — return to top">GRINDFATHER</a>

    <div class="footer__right">
      <nav class="footer__social" aria-label="Social media links">
        <a href="https://instagram.com/the_grindfather_coffee" target="_blank" rel="noopener noreferrer" aria-label="Follow on Instagram (opens in new tab)">Instagram</a>
        <span class="footer__sep" aria-hidden="true">/</span>
        <a href="https://facebook.com/p/The-Grindfather-Coffee-61575198692256" target="_blank" rel="noopener noreferrer" aria-label="Follow on Facebook (opens in new tab)">Facebook</a>
      </nav>

      <p class="footer__meta">
        <span>Cardiff · Since 2015</span>
        <span class="footer__sep" aria-hidden="true">·</span>
        <time datetime="2026">&copy; 2026 GRINDFATHER Coffee Co.</time>
      </p>
    </div>
  </div>

  <div class="footer__legal">
    <details class="legal-block">
      <summary class="legal-block__trigger">Terms &amp; Conditions · Privacy Policy</summary>
      <div class="legal-block__body">
        <p><strong>Trading:</strong> GymVision, Croescadarn Close, Pentwyn, Cardiff CF23 8HE (Mon–Fri) and Riverside Market, Fitzhamon Embankment, Cardiff CF11 6AN (Sundays). Prices in GBP incl. VAT. Cash and cards accepted. Products may contain the 14 major allergens — inform staff of allergies. Event deposits may be non-refundable; cancellations &lt;14 days may forfeit deposit. Governed by the laws of England and Wales.</p>
        <p><strong>Privacy:</strong> Data collected includes name, email, enquiry content, booking details, and analytics (IP/browser/pages). Not sold. Retained up to 2 years after last contact. Rights: access, correction, erasure, restriction, portability. ICO complaints: <a href="https://ico.org.uk" target="_blank" rel="noopener noreferrer">ico.org.uk</a> / 0303 123 1113. Contact: <a href="mailto:thegrindfathercoffee@gmail.com">thegrindfathercoffee@gmail.com</a>. Last updated 17 May 2026.</p>
      </div>
    </details>
  </div>
</footer>

<style>
  .footer {
    background-color: var(--ink);
    border-top: 1px solid var(--ink-border);
  }

  .footer__bar {
    max-width: 72rem;
    margin-inline: auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
    padding: 1.5rem 2.5rem;
  }

  .footer__wordmark {
    font-family: var(--font-display);
    font-size: 1rem;
    font-weight: 900;
    letter-spacing: 0.06em;
    color: var(--paper);
    transition: color 200ms var(--ease-out-quart);
    flex-shrink: 0;
  }

  @media (hover: hover) {
    .footer__wordmark:hover {
      color: color-mix(in oklch, var(--paper) 70%, transparent);
    }
  }

  .footer__right {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    flex-wrap: wrap;
    justify-content: flex-end;
  }

  .footer__social {
    display: flex;
    align-items: center;
    gap: 0.625rem;
    font-size: 0.6875rem;
    font-weight: 500;
    letter-spacing: 0.1em;
    color: var(--ink-muted);
  }

  .footer__social a {
    transition: color 200ms var(--ease-out-quart);
    min-height: 44px;
    display: inline-flex;
    align-items: center;
  }

  @media (hover: hover) {
    .footer__social a:hover {
      color: var(--paper);
    }
  }

  .footer__meta {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.6875rem;
    font-weight: 400;
    letter-spacing: 0.04em;
    color: var(--ink-muted);
  }

  .footer__sep {
    color: var(--ink-border);
    font-weight: 300;
  }

  /* Legal strip */
  .footer__legal {
    border-top: 1px solid var(--ink-border);
    padding: 0 2.5rem;
    max-width: 72rem;
    margin-inline: auto;
  }

  .legal-block {
    max-width: 72rem;
    margin-inline: auto;
  }

  .legal-block__trigger {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
    padding: 0.75rem 0;
    min-height: 44px;
    font-size: 0.6875rem;
    font-weight: 400;
    letter-spacing: 0.08em;
    color: var(--ink-muted);
    cursor: pointer;
    list-style: none;
    transition: color 200ms var(--ease-out-quart);
  }

  .legal-block__trigger::-webkit-details-marker {
    display: none;
  }

  .legal-block__trigger::after {
    content: '+';
    font-size: 0.875rem;
    font-weight: 300;
    color: var(--ink-border);
    transition: transform 200ms var(--ease-out-quart);
    flex-shrink: 0;
  }

  details[open] .legal-block__trigger::after {
    transform: rotate(45deg);
  }

  @media (hover: hover) {
    .legal-block__trigger:hover {
      color: var(--paper-faint);
    }
  }

  .legal-block__body {
    padding-bottom: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.625rem;
  }

  .legal-block__body p {
    font-size: 0.75rem;
    font-weight: 400;
    line-height: 1.6;
    color: var(--paper-faint);
    max-width: 80ch;
  }

  .legal-block__body strong {
    font-weight: 500;
    color: var(--paper-muted-dark);
  }

  .legal-block__body a {
    color: var(--paper-muted-dark);
    text-decoration: underline;
    text-underline-offset: 2px;
    transition: color 200ms var(--ease-out-quart);
  }

  @media (hover: hover) {
    .legal-block__body a:hover {
      color: var(--paper);
    }
  }

  @media (max-width: 640px) {
    .footer__bar {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.75rem;
      padding: 1.25rem;
    }

    .footer__right {
      justify-content: flex-start;
      gap: 1rem;
    }

    .footer__legal {
      padding: 0 1.25rem;
    }
  }

  @supports (animation-timeline: scroll()) {
    .footer__wordmark {
      animation: footer-word-in linear both;
      animation-timeline: view();
      animation-range: entry 0% entry 80%;
    }

    @keyframes footer-word-in {
      from { opacity: 0; transform: translateX(-1.5rem); }
      to   { opacity: 1; transform: none; }
    }

    .footer__right {
      animation: footer-right-in linear both;
      animation-timeline: view();
      animation-range: entry 5% entry 85%;
    }

    @keyframes footer-right-in {
      from { opacity: 0; transform: translateX(1.5rem); }
      to   { opacity: 1; transform: none; }
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .footer__wordmark,
    .footer__right {
      animation: none !important;
      opacity: 1;
      transform: none;
    }
  }
</style>
